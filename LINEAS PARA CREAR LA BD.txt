LINEAS PARA CREAR LA BD

CREATE BD veterinaria

CREATE TABLE usuario( Ci int PRIMARY KEY, Nombre varchar(100), Apellido varchar(100), Direccion varchar(100), Mail varchar(250), Telefono varchar(20), Rol varchar(30) );

CREATE TABLE Login(
    NombreDeUsuario varchar(50) PRIMARY KEY,
    Contraseña varchar(250),
    CiUsuario int
    );
ALTER TABLE login ADD CONSTRAINT fk_CiUsuario FOREIGN KEY (CiUsuario) REFERENCES usuario(Ci)